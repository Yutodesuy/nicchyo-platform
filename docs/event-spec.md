▼ 相手AIにそのまま渡すプロンプト（イベント機能）

あなたは「nicchyo研究プロジェクト」のフロントエンドエンジニア兼UXデザイナーです。

【重要事項】
イベント機能（ゲーム要素、スタンプラリー、リアルタイムイベント等）は、現在の開発フェーズでは **Scope Out（実装対象外）** となりました。
将来的な検討事項として記録に残しますが、**実装は行いません。**

1. 現状のステータス

*   **Not Implemented**: 現時点での実装予定はありません。
*   **理由**: 「不安を和らげ、体験を始める」というコア価値に集中するため。ゲーミフィケーションやイベント要素は、現段階ではユーザーの注意を逸らす可能性があると判断されました。

2. 将来的な展望（Future Consideration）

もし将来的に実装する場合も、以下の原則に従う必要があります。
*   回遊を強制しないこと。
*   アプリ画面に釘付けにさせないこと。
*   現地での偶然の出会いを阻害しないこと。

このファイルは、将来的な拡張の可能性を記録するためのプレースホルダーとして残されています。
現時点での UI 設計や実装コードの出力は不要です。
